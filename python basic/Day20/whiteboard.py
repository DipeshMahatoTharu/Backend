"""
============================================================
DAY 20 — WHITEBOARD / BLANK-PAGE CODING CHALLENGE
============================================================

Topic: Custom Stack Design (OOP + Data Structures)

------------------------------------------------------------
1. PROBLEM STATEMENT:
------------------------------------------------------------
Design a custom Stack class named `MinStack` that supports push,
pop, top (peek), and retrieving the minimum element in constant O(1) time.

------------------------------------------------------------
2. REQUIREMENTS:
------------------------------------------------------------
Implement the following functionality:
- A constructor to initialize the stack.
- `push(val)`: Pushes the element `val` onto the stack.
- `pop()`: Removes the element on the top of the stack.
- `top()`: Gets the top element of the stack.
- `get_min()`: Retrieves the minimum element in the stack.

------------------------------------------------------------
3. EXAMPLE INPUT/OUTPUT:
------------------------------------------------------------
Operations:
minStack = MinStack()
minStack.push(-2)
minStack.push(0)
minStack.push(-3)
minStack.get_min()   --> Returns -3
minStack.pop()
minStack.top()       --> Returns 0
minStack.get_min()   --> Returns -2

------------------------------------------------------------
4. CONSTRAINTS:
------------------------------------------------------------
- The methods `push`, `pop`, `top`, and `get_min` must run in O(1) time complexity.
- You may use Python's built-in lists for internal storage, but you cannot import any external libraries.
- The stack should behave gracefully when popped or peeked when empty (e.g. raise IndexError or return None).

------------------------------------------------------------
5. TEST CASES:
------------------------------------------------------------
Verify your class using these scenarios:
1. Push positive and negative integers and verify get_min() updates correctly.
2. Pop elements and verify get_min() reverts to the previous minimum.
3. Handle single-element stacks.

============================================================
MY APPROACH:
============================================================
Write your design plan, helper structures, and complexity explanation here:

____________________________________________________
____________________________________________________
____________________________________________________


============================================================
MY CODE:
============================================================
Write your complete class and method implementations here:

____________________________________________________
____________________________________________________
____________________________________________________
____________________________________________________
____________________________________________________
____________________________________________________
____________________________________________________
____________________________________________________
____________________________________________________
____________________________________________________

"""
