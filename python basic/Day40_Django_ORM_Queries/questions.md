# Day 40 — Django ORM Queries Conceptual Questions

Write your answers in the designated spaces below each question.

---

### 40.1 QuerySet Lazy Evaluation & Memory Caching
**QUESTION:**
Explain how Django caches QuerySet results in memory. Under what circumstances will iterating over a QuerySet twice hit the database once vs twice?

**MY ANSWER:**
____________________________________________________
____________________________________________________
____________________________________________________

---

### 40.2 Race Conditions & `F` Expressions
**QUESTION:**
Explain the "Lost Update" problem in database concurrency. How does an `F()` expression translate to SQL, and why does it prevent race conditions without requiring distributed locks?

**MY ANSWER:**
____________________________________________________
____________________________________________________
____________________________________________________

---

### 40.3 `exists()` vs `count()` vs `len()`
**QUESTION:**
What are the exact SQL queries generated by `if product_qs.exists():`, `if product_qs.count() > 0:`, and `if len(product_qs) > 0:`? Which one is optimal when you only care about presence?

**MY ANSWER:**
____________________________________________________
____________________________________________________
____________________________________________________
